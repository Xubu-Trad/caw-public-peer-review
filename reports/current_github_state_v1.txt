REMOTE_REVIEW_REPO
{"description":"Public reproducible peer-review record for Gilgamesh CAW build vs CAW manifesto alignment.","nameWithOwner":"Xubu-Trad/caw-public-peer-review","repositoryTopics":[{"name":"a-hunters-dream"},{"name":"caw"},{"name":"decentralization"},{"name":"forensics"},{"name":"layerzero"},{"name":"manifesto"},{"name":"peer-review"},{"name":"smart-contract-review"}],"url":"https://github.com/Xubu-Trad/caw-public-peer-review"}

LOCAL_REVIEW_REPO
 M docs/public_summary_draft.md
?? reports/current_claims_audit_2026-05-10.md
?? reports/current_github_state_v1.txt
72da07d (HEAD -> main, origin/main) refresh ledger after current state amendments
1b7f28e tighten claims matrix with current repo state
37c550f add current state amendment for Gilgamesh repo activity
03f4808 refresh repo publish status after metadata update
6e25e75 add repo publish status
6410e87 add repo publish status
6a1c8d3 refresh sha256 ledger after final repo files
909717b add public summary draft
d73f191 add public challenge
0db638b add public review issue templates
5002d97 add reproducibility guide
3c4a948 add sha256 provenance ledger

GILGAMESH_REMOTE_PUBLIC_STATE
{"defaultBranchRef":{"name":"master"},"description":"","forkCount":5,"isFork":true,"nameWithOwner":"GilgameshCaw/Caw","parent":{"id":"R_kgDOHcAdOQ","name":"CawUsernames","owner":{"id":"U_kgDOBcWirw","login":"MasterSprouts"}},"stargazerCount":52,"url":"https://github.com/GilgameshCaw/Caw"}

GILGAMESH_LATEST_COMMITS
b39a23a0f8212090e2730cee750b43eb917aa2b2	2026-05-16T07:59:09Z	perf(notifications): persistent rollup buckets, read becomes O(page)\n\nReplaces the read-time GROUP BY aggregation introduced last commit\nwith a write-time persistent rollup. NotificationGroup is now the\nauthoritative feed source.\n\nGroup semantics:\n\n  - Identity = (userId, type, targetKey). targetKey is the cawId for\n    content actions, offerId for OFFER, null for FOLLOW (one global\n    follow bucket per user) + ACTION_FAILED (each row its own group).\n  - A new notification joins the currently OPEN group on its bucket.\n    A group is open when isRead=false AND lastEventAt is within the\n    last 15 minutes; otherwise the write opens a fresh group.\n  - Marking a group read closes it: the next event on the same\n    bucket starts a fresh group. Users see "5 new likes" then later\n    see a distinct "1 new like" instead of one ever-growing rollup.\n\nWrite path:\n\n  - New helper createNotificationWithGroup() wraps Notification.create\n    and attaches/creates the group atomically. Returns the new\n    notification id.\n  - Wired into every NotificationService.create*Notification() call\n    (FOLLOW, LIKE, REPLY, REPOST, QUOTE, TIP, MENTION). Mention's\n    createMany is now a per-row loop through the helper so each\n    recipient gets the right group.\n  - Wired into the out-of-service callsites: txQueueFailure\n    (ACTION_FAILED), /api/marketplace (OFFER), MarketplaceIndexer\n    (SALE_SOLD, SALE_BOUGHT, OUTBID, AUCTION_WON), and the test\n    /api/notifications POST.\n\nRead path:\n\n  - GET /api/notifications now queries NotificationGroup directly,\n    paginates by lastEventAt DESC on the indexed (userId,\n    lastEventAt) lookup. No per-request aggregation.\n  - GET /api/notifications/unread-count counts unread GROUPS (the\n    bell badge semantic matches what the feed displays).\n  - NotificationService.markAsRead flips both the Notification row\n    and the parent NotificationGroup. For partial group reads (user\n    clicks individual rows), the group stays unread until all members\n    are read.\n\nMigration:\n\n  - Creates NotificationGroup table + two indexes (one for the feed\n    read, one for the open-group write lookup).\n  - Adds Notification.groupId FK with ON DELETE SET NULL.\n  - Backfills every existing notification into a group: greedy walk\n    in (userId, type, createdAt) order, opens a new group whenever\n    the gap to the previous same-bucket row exceeds 15 minutes OR\n    the prior group was already marked read. Final reconcile pass\n    sets each group's isRead based on whether ALL its members are\n    read.\n\nReplaces the SQL aggregation hot path; reads on heavy users (~4K\nnotifications) drop from hundreds of ms to a single indexed lookup.\n\nDisplay-name DB corruption (e.g. "Jonathan JohnsonGrace Wilson") is\nstill open — same surfaced finding as the previous commit, not\naddressed here.
2959b7ec1dc2e2686c8402ab9695405b95e09026	2026-05-16T07:38:15Z	fix(notifications): paginate by GROUP, not by raw row\n\nLoad more on the notifications page kept returning the same 3 rolled-\nup rows with a different lead actor each time. The bug: offset-based\npagination of RAW rows then in-memory grouping. Each page sliced a\ndifferent subset of the same 'follow' bucket (groupKey='follow' for\nALL follow notifications on a user), so each page produced a\ndifferent rollup row from the same source data. Users with 99 unread\nnotifications saw the same 3 groups repeating forever past Load\nmore.\n\nSwitched to group-aware pagination via SQL aggregation:\n\n  - Query 1 (raw SQL): GROUP BY COALESCE(groupKey, 'single:'||id)\n    over the user's visible notifications, returning each group's\n    latest_id, total_count, latest_created_at, any_unread. Paginated\n    by latest_created_at DESC with LIMIT + OFFSET. Notifications\n    without a groupKey get a per-row synthetic key so they each form\n    their own group.\n\n  - Query 2 (Prisma): fetch the representative row (latest in each\n    group) with actor / caw / offer relations for rendering.\n\n  - Query 3 (raw SQL DISTINCT ON): fetch up to 50 distinct\n    additional actors per multi-row group for the rollup avatar\n    stack + the "and N others" link.\n\n  - hasMore now reflects GROUP count vs limit (not raw row count vs\n    limit) so the UI stops asking for the next page when there are\n    no more distinct groups to show.\n\nPerf: added a composite index Notification(userId, groupKey,\ncreatedAt) so the GROUP BY can use an index-only scan instead of a\nfull scan + HashAggregate. Heavy users (~4K notifications) were\notherwise hitting hundreds of milliseconds per page load. Migration:\nCREATE INDEX IF NOT EXISTS, rides the deploy.\n\nDisplay-name DB corruption (e.g. "Jonathan JohnsonGrace Wilson") is a\nseparate issue — surfaced for cleanup, not addressed here.
75be412ae849be0d8d77be8e58891261d68580c3	2026-05-16T07:24:52Z	fix(drawer): bump mobile menu above page-level floating buttons\n\nOpening the mobile drawer on a user profile showed the page's back\nbutton (Profile.tsx z-[80]) floating on top of the drawer's menu\nitems. The drawer backdrop was z-[70]. Same shape applied to the\nCawPage and Messages header buttons that also sit at z-[80].\n\nBumped the drawer backdrop to z-[90] so page chrome at z-[80] gets\ncovered when the drawer is open. The mobile header (z-[9999]) still\nsits above the drawer so its hamburger / X toggle stays tappable for\ndismiss.
95889803451c19d19ec7955734db5ecac267cbe0	2026-05-16T07:09:29Z	feat(polls): on-chain voting window via ::pd:<duration>:: marker sidecar\n\nPolls now carry a voting window in their on-chain marker. Closes #145.\n\nMarker format gains an optional duration sidecar that lives directly\nafter the existing image sidecar (or right after the main marker when\nno images):\n\n  ::poll:a:b:c::pd:7d::\n  ::poll:a:b::pi:host:hashes::pd:1h::\n\nFormat: ::pd:<n>(h|d|w):: bounded to 1h..30d. Composer choices:\n1h / 6h / 1d / 7d / 30d (default 1d).\n\nWire-up:\n\n  - pollMarker.ts gains POLL_DURATION_CHOICES + parsePollDuration() +\n    ParsedPoll.durationSeconds, and buildPollMarker accepts a\n    duration arg. Sidecar parsing walks ::pi:: then ::pd:: in order,\n    each independently optional.\n\n  - Poll model gets `endsAt: DateTime?`. NULL = legacy poll\n    (pre-sidecar), never expires. Migration: ADD COLUMN IF NOT EXISTS.\n\n  - Indexer (handleCawAction) computes endsAt from caw.createdAt +\n    durationSeconds when the marker carries one. Mirror-consistent\n    because every node derives the same value from the same on-chain\n    inputs.\n\n  - Indexer (handleVoteAction) rejects votes when Date.now() is past\n    poll.endsAt. Legacy null-endsAt polls always accept votes.\n\n  - API (/api/actions optimistic vote path) returns 409 POLL_ENDED\n    for late votes so the FE doesn't write a fake "your vote landed"\n    state that the indexer will then reject.\n\n  - FE PollComposer: duration <select> next to the existing position\n    picker, plumbed through PostForm → buildPollMarker.\n\n  - FE PollDisplay: shows "Ends in 3d 4h" / "Poll ended" inline next\n    to the vote count. Vote buttons disabled when ended. Counts +\n    bars stay visible (showResults now also true for ended polls so\n    viewers can see the final tally without having had to vote).\n    Footer countdown re-renders every 30s; cheap, only fires for\n    polls with endsAt set.\n\nMigration rides the deploy. ABI surface unchanged (marker is text-\nencoded inside caw content).
2064f86083c0ba3a34fcbe28c5869813d46cc7f1	2026-05-16T06:50:17Z	fix(auth): auto-refresh session on token-owner change, no re-sign\n\nAfter buying an NFT in the marketplace, the user couldn't post / like /\nrecaw with the newly-owned profile until they manually signed back in.\nThe session's authorizedTokenIds was captured at sign-in time and\ndidn't pick up the new tokenId, so requireAuth({ verifyOwnership })\nreturned 403 TOKEN_OWNER_CHANGED.\n\nTwo wins:\n\n  - BuyModal: once refetchTokenDataUntilChanged settles (indexer has\n    written the new owner address), call /api/auth/refresh. That\n    endpoint re-reads the DB's address→tokenId mapping and updates\n    the session in place — no new signature, no UX prompt.\n\n  - apiFetch: any 403 with error=TOKEN_OWNER_CHANGED triggers a\n    one-shot /api/auth/refresh + retry. Covers the offer-accept path\n    too (the buyer learns asynchronously, can't intercept the buy\n    flow; the first failing action now auto-recovers instead).\n    Guarded by a private retry flag so a stuck 403 can't loop.\n\nTogether: the buy → post-with-new-profile path no longer requires a\nre-sign-in. The race window between buy-tx-confirmed and\nindexer-caught-up still exists but now self-heals on the next action.\n\nCloses bug report #135.
ba054fff17feb93871aab2217bba5ada3b18eec4	2026-05-16T06:45:53Z	fix(post-form): stop scrolling 15px per keystroke in the reply box\n\nThe autoResize effect re-measured content height by collapsing the\ntextarea (`height='0px'`) and re-reading scrollHeight on the next\nframe. Even though the collapse only lasted one paint, iOS Safari\n(and Chrome to a lesser degree) used that brief empty layout to\nre-anchor the caret, which scrolled the document ~15px on every\nkeystroke until the caret reached the bottom of the viewport.\n\nSwitched to a hidden measurement mirror — a position:absolute,\nvisibility:hidden <div> with the same width/font/padding/wrap as the\ntextarea. Its offsetHeight is the textarea's natural unconstrained\nheight, so we can set the new height directly with no transient\ncollapse. Pure measurement, never visible, aria-hidden +\npointer-events-none.\n\nCloses bug report #211 and the related per-keystroke scroll\nbehaviour reported on the reply form.
2ce936f45010c07d057c97b49d44722f4ef4d43f	2026-05-16T06:36:27Z	fix(actions): CawActionsERC1271 trailing-bytes + withdraw-cap requires\n\nTwo correctness fixes flagged by the post-merge audit\n(messages/erc1271-sibling-fixspec.md):\n\n- Trailing bytes: processActionsERC1271 now asserts ctx.pos ==\n  packedActions.length after the group loop, mirroring the existing\n  check at CawActions.sol:243. Without it, two semantically-identical\n  batches with different trailing bytes would emit different batchHash\n  values, breaking indexer dedupe and archive submission uniqueness.\n- Withdraw cap: new _recordWithdraw helper enforces ctx.withdrawCount\n  < 256 before each indexed write into the fixed-size memory arrays.\n  Adversarial stress case but a friendly error beats a bare bounds\n  revert.\n\n3 new tests added; full suite at 278 passing. Sibling bytecode at\n5,340 / 24,576 — 19K+ headroom remaining.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
5d40d5f60eab164769e5e754562647b551d44680	2026-05-16T06:28:41Z	fix(post-form): never crash the form on a regex parse failure\n\nBug report #82 described "first character visible, second onwards\nblack, then the screen goes black with 'an error occurred'". Two\ndistinct symptoms — the most plausible upstream cause for the screen-\ngoes-black-with-error part is the highlight regex throwing on some\nedge case and killing the component tree.\n\nWrap getHighlightedText in a try/catch so a malformed regex run\nfalls back to plain text instead of bubbling to the nearest React\nerror boundary. If this turns out to be the trigger we close #82;\nif not, we've still removed an unrecoverable failure mode from the\npost form. Logs a warn so a real recurrence shows up in console.
3a5519b58f358b5e80d3807d9021e3179ba0f249	2026-05-16T06:26:26Z	fix(media-upload): stop the dragged image from re-adding itself\n\nDragging a preview cell to reorder triggered the wrapper-level OS\nfile-drop handler too. Chrome synthesizes a File entry in\ndataTransfer.files when an inline <img> is the drag source, so the\nwrapper's handleDrop re-processed the cell as a fresh upload — every\ndrag added another copy of the same image until the 4-image cap\nlocked things up.\n\nThree guards together close the path:\n\n  - handleReorderDrop now stopPropagation so the inner reorder event\n    doesn't bubble to the wrapper handler at all.\n  - handleDrop bails early when draggedIndex !== null (defense in\n    depth — covers drops that land on the wrapper edge instead of a\n    cell).\n  - handleDragOver also bails during a reorder so the wrapper's\n    "drop new files here" yellow-dashed highlight doesn't appear and\n    confuse the user about what's about to happen on release.\n\nCloses bug report #127.
6b2845fa85ad37b5b5c354d9d2d68c196e89b4bc	2026-05-16T06:18:09Z	fix(post-video): pause on scroll-off + one-video-audio coordinator\n\nThree reported video player issues, all caused by PostVideo instances\nrunning fully isolated — no scroll awareness, no shared audio state.\n\n  - #177 (videos kept playing after swipe-down): each video now\n    attaches an IntersectionObserver at threshold 0.25. Out-of-view\n    pauses; back-in-view auto-resumes (unless the user explicitly\n    paused, tracked via videoUserPausedRef).\n\n  - #173 / #99 (two videos audible at once): module-scoped\n    audioCoordinator tracks the single video currently playing with\n    sound. claimAudio() re-mutes the previously-tracked element\n    before installing the new one. Wired into:\n      - toggleMute (desktop overlay)\n      - volumechange listener (catches the mobile native-controls\n        path AND any programmatic mute)\n      - IntersectionObserver exit (force-mute + release so a\n        previously-unmuted video doesn't auto-resume with sound)\n\n  - The mobile path was missing `ref={videoRef}` entirely, so its\n    pause / mute / scroll behavior was completely unwired. Added the\n    ref and matching onPlay/onPause handlers; the desktop-only state\n    (currentTime/duration) is harmless to track on mobile (the\n    custom overlay doesn't render there).\n\nCloses bug reports #177, #173, #99.
af94408879a553f4fd49e2de7495b20cf52ce63c	2026-05-16T06:09:10Z	chore(deploy): wire CawActionsERC1271 sibling into multi-chain deploy\n\nAdds CawActionsERC1271_L1 + per-L2 CawActionsERC1271_${L} entries to the\nCONTRACTS map, with a predictedSiblingKey mechanism on CawActions entries\nso the immutable _erc1271Sibling ctor arg gets the sibling's predicted\nCREATE address (deployer nonce + 1). LINKING_STEPS adds one-shot\nsetERC1271Sibling() calls per CawProfileL2, gated by an on-chain skipIf\ncheck so re-runs are idempotent. deployments.ts blocks extended to\nsurface the sibling address per chain for backend / FE consumers.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
7635663ce3deefb637ad30a85f96a0ffdb51116b	2026-05-16T06:08:48Z	fix(nav): mobile nav + FAB pass taps through while scrolling\n\nWhen the user scrolled, the bottom mobile nav dropped to opacity-30\nso it wouldn't visually obstruct reading. Problem: the semi-\ntransparent nav kept its pointer events, so taps over the post\naction row sitting behind the nav landed on the nav instead. The\nicons were visible through the nav so the row looked interactive,\nbut tapping Recaw/Like/etc did nothing useful.\n\nAdd `pointer-events-none` to the same scroll-active state on both\nthe bottom nav and the new-post FAB. Both reclaim pointer events\n180ms after the last scroll event (existing scroll-idle timer), so\nthe nav buttons themselves stay responsive whenever the user has\nactually stopped scrolling and intends to tap a nav target.\n\nCloses bug report #215.
1add72e08557e2dfa39634ad8a7959bee3511886	2026-05-16T05:06:10Z	fix(notifications): grouped-actor i18n + mark-all-read header overflow\n\nTwo reported issues, both on the notifications screen:\n\n  1) Grouped-actor rows leaked the raw {{name}} placeholder in any\n     non-English locale: "Liam Rodriguez and {{name}} y 8 más le\n     dieron me gusta a tu caw". Two stacked bugs:\n\n       - The split between the actor name and the count phrase was\n         done via `othersLabel.lastIndexOf(' and ')` — hardcoded\n         English. Spanish (' y '), German (' und '), Japanese (' と他 '),\n         etc. never matched, so the entire template (including the\n         literal {{name}}) fell through as the button text.\n       - The connector rendered between actor and button was the\n         same hardcoded ' and ', so even when the split happened to\n         work the connector was wrong in non-English.\n\n     Fix: indexOf('{{name}}') on the raw template, then split the\n     trailing text into (connector + count phrase) via a regex that\n     works across whitespace conventions and scripts.\n\n  2) "Marcar todas como leídas" wrapped to two lines on narrow\n     mobile and overlapped the page title. Shortened the ES string\n     to "Marcar todas" — the ✓ icon + button context still reads\n     unambiguously — and added whitespace-nowrap + flex-shrink-0 to\n     the button, plus min-w-0 truncate to the title, so the same\n     overflow can't happen in other long-label locales (pl, ru, de,\n     nl all run noticeably long here).
30cba31e75918e6930ce9f66b446f0cdf91b0999	2026-05-16T05:05:35Z	feat(actions): CawActionsERC1271 sibling contract for passkey/smart-EOA owners\n\nAdds a sibling contract that processes variable-length WebAuthn / ERC-1271\nsignatures from contract-owned profiles. The wire format used by\nprocessActions is fixed 67 bytes per group and cannot carry the 200-400-byte\nWebAuthn assertion; the sibling exposes processActionsERC1271() which takes\nbytes[] sigs alongside packedActions, verifies isValidSignature() on each\nowner, and calls back into CawActions.processGroupSingle() in preVerifiedSigner\nmode.\n\n- CawActions: new immutable erc1271Sibling, 4th constructor arg, extended\n  processGroupSingle with preVerifiedSigner param (sibling-mode skips sig\n  verification since the sibling has already validated).\n- CawProfileL2: erc1271Sibling state slot with one-shot setter (revert if\n  already set, revert on zero address). setWithdrawable accepts cawActions\n  OR erc1271Sibling.\n- One canonical hash chain, one cawonce bitmap, one ActionsProcessed event\n  stream. Validators batch ECDSA actions in processActions, ERC-1271 actions\n  in processActionsERC1271 — separate txs, single-batch amortization\n  preserved within each tx.\n\nCritical for passkey users: WITHDRAW cannot be delegated to a session key,\nso without this sibling, passkey-backed owners would have no path to\nwithdraw their CAW.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
b0cd85052e8a5aa4a625b83677c328ef43b45214	2026-05-16T04:44:19Z	feat(networks): add per-Network feeCeiling, set at createNetwork, lower-only\n\nNetwork operators now commit to a maximum fee ceiling at createNetwork.\nAll four fee setters (withdraw, deposit, auth, mint, plus the batch setFees)\nrevert if the new value exceeds the ceiling. lowerFeeCeiling() ratchets the\nceiling down — never up — and refuses values below any current fee. The\nceiling can also be 0 for a permanently-free network. 26 new tests cover\nevery gated path.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
07d2075dd0b7fd37d1cad52c30777fe2aa0a82c6	2026-05-16T04:25:00Z	fix(post-form): keyboard no longer covers Post button, kill duplicate placeholder\n\nTwo reported post-form bugs:\n\n  #136 — On mobile the iOS / Android keyboard slid over the Post\n         button. The inline-compose toolbar pins to bottom:0 with\n         safe-area padding, but iOS Safari and Chrome only respect\n         that against the keyboard when the layout viewport actually\n         shrinks. The viewport meta tag was missing\n         interactive-widget=resizes-content, so the keyboard floated\n         over the fixed toolbar instead. Added it. Browsers older\n         than iOS 16.4 / Chrome 108 ignore the unknown token.\n\n  #143 — Posting an image with no text showed the placeholder twice\n         — once from the textarea's native ::placeholder and once\n         from HighlightedTextarea's overlay div. The textarea text\n         itself is transparent (the overlay renders a colored copy\n         for hashtag / mention highlighting), but the native\n         placeholder kept rendering on top of the overlay. Added\n         placeholder-transparent to the textarea so only the overlay\n         placeholder is visible. The placeholder= attribute stays on\n         the element for a11y / form-fill.
240782f01771b2e46fdca12bd83790000bc44ca0	2026-05-16T04:21:22Z	fix(caw-page): full cursor pagination for likes/recaws/tips\n\nReverts the cheap-fix 500 cap. The interactions tabs now paginate\nproperly so there's no hard ceiling on viewable likers/recawers/tippers.\n\nServer:\n  - New GET /api/caws/:id/recaws and /:id/tips, both with cursor\n    pagination shaped like the existing /likes endpoint.\n  - Inline recaws/tips on /api/caws/:id keep a 100-row initial page\n    plus hasMoreRecaws / recawsNextCursor / hasMoreTips / tipsNextCursor\n    so the post page can paint immediately and only round-trip when\n    the user actually scrolls past the first page.\n  - Both inline queries switch to orderBy id asc to match the cursor\n    field (createdAt has ties; id is the unique total order).\n\nFE:\n  - CawDetailResponse type centralizes the response shape across the\n    three /api/caws/:id callsites.\n  - loadMoreRecaws / loadMoreTips mirror loadMoreLikes.\n  - Reposts and Tips tabs render a Load more button when the server\n    reports more pages available.\n  - Cursors reset on caw-id change so a stale cursor can't page a\n    different post's data.\n\nTab counter source-of-truth fix:\n  - Reposts label was using `recaws.length + quoteIndicators.length`\n    and Tips was using `tips.length`. Both undercount once any single\n    page is full. Switched to caw.recawCount / caw.tipCount /\n    caw.commentCount / caw.likeCount — the authoritative counters\n    already populated by the indexer + tip aggregate. Drops the dead\n    commentIndicators / quoteIndicators memos.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
78b795a8ad1946bdd71fe7edbf0810d9064cbefe	2026-05-16T04:13:47Z	ui(dm-inbox): emphasize conversations with unread messages\n\nConversations with unreadCount > 0 now stand out more on the index:\n\n  - Title: font-bold (vs. semibold) and tinted brand yellow\n    (text-yellow-400 dark / text-yellow-600 light) — same color\n    family as the existing unread badge and the "Load more" CTA.\n  - Preview: bumped from text-gray-300/700 to text-white/black at\n    font-semibold so the message line itself reads as new.\n  - Timestamp: tinted yellow when unread, matching the title.\n\nThe numeric badge is unchanged.
a8d7e01117f25618c6508835c17d951e85e50851	2026-05-16T04:06:34Z	fix(ux): bug-bash round — copy button hit area, right sidebar bg, dropped-media toast\n\nThree small reported issues from the pending bug queue:\n\n  - CopyAddressButton: expand the touch target via p-1.5 / -m-1.5\n    (visual layout unchanged) and bump the icon from w-3 to w-3.5\n    so the button is reachable on mobile address pages. (#124)\n\n  - MainLayout right sidebar: pin bg-black in dark mode. Without a\n    background fill the panel inherited the page background and\n    read as floating text rather than a sidebar. (#178)\n\n  - PostForm media picker: when iOS lets the user pick more than the\n    4-image / 1-video cap, surface a toast instead of silently\n    dropping the extras. (#75)\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
494752d6a175bac6bf78f0a58e6c5c426bddc557	2026-05-16T04:02:04Z	fix(caw-page): paginate likes list + raise recaws/tips caps\n\nThe likes tab on /caws/<id> only ever fetched the first 100 rows\nbecause the FE didn't pass the API's existing cursor through, so\nposts with more than 100 likes hid everyone past the first page.\nRecaws and tips had a hard `take: 100` server-side with no pagination\nat all — same symptom, but on the inline timeline rendering.\n\n  - CawPage: thread nextCursor through the likes fetcher; bump per-\n    page request to the API's 500 cap; render a Load more likes\n    button when more is available.\n  - /api/caws/:id: raise the inline recaws and tips caps from 100 to\n    500. Still a hard cap (no paged endpoint for those yet), but\n    pushes the cliff well past anything we see in practice.\n\nCloses bug report #286.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
9534f44676cbce519c564a2577d155b57d8f894d	2026-05-16T03:56:27Z	fix(dm): reserve vertical space for images before they load\n\nConversation scroll jumped every time a DM image finished loading\nbecause the bubble had no aspect-ratio hint until the bitmap mounted.\n\n  - EncryptedImage now accepts width / height props. The upload path\n    has captured these via readImageDimensions since 2026-05-12, so\n    every new attachment will plumb through naturally; pre-rollout\n    attachments fall back to the existing 200x150 skeleton.\n  - The loading placeholder mirrors the eventual image's aspect ratio\n    (clamped to 240px), and the <img> gets width/height attributes so\n    the browser reserves the right box pre-paint.\n  - For external/giphy URLs (no metadata), inline a 240px-wide 4:3\n    aspect hint that clears onLoad so the GIF's natural ratio takes\n    over without locking the box.
fb1b86ab4c73d161f444e2175170126904c0de62	2026-05-16T03:50:41Z	fix(pin): suppress unpinned posts during PENDING window\n\nPin/unpin both wrote pending=true on PinnedCaw, so the read path\ncouldn't tell a pin-in-flight from an unpin-in-flight. Result: after\nan optimistic unpin, a refresh re-fetched the still-existing row and\nthe post snapped back to the top of the profile feed with the pin\nbadge — staying there until the indexer deleted the row on confirm.\n\nSplit the lifecycle:\n  - `pending`        — pin in flight (existing behavior).\n  - `pendingUnpin`   — confirmed pin with an xpi: in flight; row still\n                       exists so the indexer can delete on confirm,\n                       but read paths suppress it.\n\nWired through:\n  - schema + IF NOT EXISTS migration\n  - /api/actions xpi: sets pendingUnpin; pi: clears it on re-pin\n  - /api/caws profile feed filters pendingUnpin: false\n  - /api/pins (off-chain fallback) clears pendingUnpin on re-pin\n  - txQueueFailure rolls back pendingUnpin on terminal failure\n    instead of the old (now-incorrect) pending flip\n\nIndexer paths in actionHandlers.ts unchanged: handleUnpinAction still\ndeletes the row outright (covers both pendingUnpin and bare-confirm\ncases); handlePinAction's pending → confirmed transition is\nunaffected.\n\nCloses bug report #208.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
affe7157cde2fe00c3f9eeb56d6d92076695b9c8	2026-05-16T03:37:29Z	feat(notifications): clickable "X others" opens actor list modal\n\nGrouped follow/like/repost/tip notifications now render "X others" as a\nbutton that opens FollowListModal in a new 'notificationActors' mode.\nThe modal paginates via the GET /api/notifications/group-actors endpoint\nadded in 0ce2d61d. Reuses the existing followers/following list UI.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
6cd5d9cc21fa343e9d823880c6dea52557cf2e40	2026-05-16T03:34:46Z	fix(dm): emoji picker inserts at cursor, not end of message\n\nBoth DM composer emoji paths (the inline 13-emoji strip and the full\nEmojiPickerModal) used `setNewMessageContent(prev => prev + emoji)`,\nwhich always appended. Picking an emoji mid-message dropped it at the\nend. Splice at the cursor via the existing composerCursor /\ncomposerTextareaRef plumbing, mirroring handleMentionSelect.\n\nCloses bug report #283.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
0ce2d61d4e98daf17c793b3bbcaa7e57be519da8	2026-05-16T03:33:29Z	feat(notifications): group FOLLOW notifications server-side\n\nFollows now collapse into one row per recipient via groupKey: 'follow',\njoining the existing LIKE/REPOST/TIP grouping path. additionalActors is\ncapped at 50 per group (count stays exact); a new\nGET /api/notifications/group-actors endpoint paginates the full actor\nlist for the FE modal. No schema migration; groupKey column existed.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
f9f94d6b79a1d447dcc5f327d86c7f5fa6e75a08	2026-05-16T02:42:34Z	fix(cli): escape backticks in nginx.js template literal\n\nThe renderNginxConf template literal contained 8 unescaped backticks\ninside nginx-comment lines (around `?`, `%3F...`, `rewrite ... last;`).\nBackticks-balance to even so it parsed pre-b49bb9a5, but everything\nbetween consecutive ticks was getting evaluated as JS — node 22 finally\ncaught it as "Invalid or unexpected token" on the `#` at column 1 of\nthe next line. Made `caw update` unrunnable from the VPS install since\nb49bb9a5 landed, so the i18n / SP1 / health-probe payload pushed today\nnever reached the running services.\n\nEscaped all eight as \\\\\\` so they stay literal backticks inside the\ntemplate.
c7c3fd0c96ff9e07c4bd4c164c420f4ec14944c3	2026-05-15T20:22:35Z	feat(i18n,ux): expand i18n coverage + UX fixes across feed/profile/menus\n\ni18n — extracted hardcoded English strings to t() and added EN/ES\ntranslations across the components below. The other 20 locales are\nfilled via the new scripts/i18n-fill.mjs helper (Google Translate\npublic endpoint, {{vars}} preserved, EN fallback on failure). Closes\n#279 (Japanese locale incomplete coverage):\n\n- QuickSignHowItWorks: extracted hardcoded strings to t() (component\n  previously had no useT import).\n- ProfileEditForm: 6 field labels (Profile Picture / Cover Photo /\n  Display Name / Description / Website / Location) + chars-remaining.\n- Messages: New Message modal (search hint, recent follows, send/empty\n  states), Message Settings modal (privacy options + group invites +\n  Done), Can't Send Message modal, inbox/requests tabs + search +\n  encryption label, "New direct message" / "New group chat" menu.\n- CreateGroupModal: extracted hardcoded strings to t() (component\n  previously had no useT import). Local `t` shadow inside setTimeout\n  effect renamed to `to`.\n- Staking + StakingRewardsInfo: deposit/withdraw panels, portfolio\n  overview cards, info accordion (Requirements / Reward Distribution\n  / Real-time Rewards), active account label, rewards table.\n- Marketplace + ListingFilters: filters (Type / Payment / Length /\n  Sort + all option labels), Offers Sent / Received sections,\n  Cancel & Withdraw / Deny buttons.\n- Profile 3-dot menu: Share profile / List for Sale / View Offers /\n  Transfer Profile.\n- Explore + Trending: "What's trending" and "Trending" titles.\n\nFix #267 / #269 — User search shows "0 followers". The /api/search\nendpoint was selecting username/displayName/avatar fields but not\nfollowerCount, so the FE always read undefined → fell back to 0. Adds\n`followerCount: true` to both the ES branch and the Prisma fallback\nselect in client/src/api/routes/search.ts.\n\nFix — AccountSettings wallet/profile rows showed deterministic default\navatars instead of real ones because TokenData has no avatar fields.\nMirrors ProfileChooser's pattern: hydrate avatarsByTokenId on mount\nvia /api/users/:username, then inject avatarUrl when rendering each\nUserAvatar.\n\nFix — FollowListModal avatars rendered as oval/squashed when\nusernames were long because the w-10 h-10 wrapper had no flex-shrink-0\ninside its flex row. Adds flex-shrink-0 so width stays 40px.\n\nFix — Messages composer GIF preview had a redundant "Send GIF" button\nthat broke the muscle-memory established by the image preview (which\nsends via the single composer send button). Removes the extra button\nand wires gifPreview into the main send button + Enter key handler,\nmatching the file-preview pattern verbatim.\n\nFix #262 — Hamburger menu icon flipped to X but the side panel never\nappeared. A phantom inline style.transform left by a half-finished\nswipe-drag could override the class-based translate-x toggle. Calls\nclearInlineDrawerStyles() before toggling, and switches to a\nfunction-form setter for the open state.\n\nFix #263 — iOS back-arrow + X-profile-link unresponsive. The mobile\nedge-swipe catcher (fixed strip at the left edge) was intercepting\ntaps on left-edge buttons via a higher-priority stacking context.\nNarrows the strip from w-5 (20px) to w-2 (8px) — still catches the\nnative edge-swipe gesture, no longer overlaps button hit areas.\n\nFeat #280 — Horizontal swipe on the home feed toggles between\nFollowing / For You (X-style). Axis-locked after the first 10px so\nvertical scroll wins; commits at |dx| >= 60px. Lets users switch tabs\nwithout scrolling back to the top.
8d2581ef6506d526c3584a582768249a27ab2aa0	2026-05-16T01:59:48Z	chore(deploy): wire SP1 verifier + program vkey into CawActions ctor\n\nCawActions's _zkVerifier and _zkProgramVKey are immutable. Until now\ndeploy passed only the CawProfileL2 address, leaving _zkVerifier =\naddress(0) and processActionsWithZkSigs reverting on every call.\n\nThis change adds the sp1Verifier field to every CHAINS entry, a\nZK_PROGRAM_VKEY constant with a lockstep check against the SP1 fixture,\nand a requireSp1Verifier() guard that refuses placeholders. Dev chains\ndeploy MockSP1Verifier at phase 1 via the existing condition mechanism.\nBase Sepolia is wired to the canonical Succinct address\n(0x397A5f7f3dBd538f23DE225B51f532c34448dA9B); other prod chains carry\n'<TBD ...>' placeholders that abort the deploy until filled in.\n\nNo contract changes. No ABI changes.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
5e97b1678d3afd887d3a7981d0b504a36d83aae0	2026-05-16T01:44:22Z	chore(scripts): add health probes for txqueue + bugreports\n\n/loop-able read-only DB checks for VPS monitoring. 20-minute window\non TxQueue (failed counts, stuck rows, cawonce-used storm count) and\nBugReport (new PENDING last 20m, top reporters last 24h). Exit code 1\nwhen action required.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
a51ac9c3bb23024f038ad261b3d1462f1d69f85e	2026-05-15T12:06:44Z	fix(caws): 404 instead of 500 when route id isn't a real caw id\n\nGET /api/caws/:id was throwing on Prisma findUnique({ id: NaN }) when\nthe path segment wasn't a positive integer — turning every external\nshare of an FE-generated `pending-<ms>-<rand>` tempId URL into a 500.\nProduction hits: 5 in 24h from different IPs/UAs all pointing at one\nshared tempId (someone pasted a /caws/pending-... URL into a Line chat\nbefore the optimistic post had flipped to its real id).\n\nBackend: validate Number.isInteger + >0 before the query; return 404\non garbage (matches the no-such-caw semantic, lets the FE render a\nclean tombstone instead of a noisy "could not load").\n\nFrontend: CawPage already guarded loadCaw against tempId routes, but\nthe likes-fetch and refreshComments paths didn't — both would have\n404'd silently after this commit, but they shouldn't be making the\nrequest at all (no DB row exists for a tempId). Add the same\nisTempIdRoute early-return so the network panel stays clean.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
1e9ea73b73c5fded108ad056fe937c273373d4cd	2026-05-15T08:18:55Z	docs(CLAUDE): clientHashAtCheckpoint is already renamed to networkHashAtCheckpoint\n\nThe "legacy storage name, not yet renamed in code" annotation was stale —\nthe rename actually landed in commit a72ca70a along with the rest of the\nclient→Network contract pass. Update the two CLAUDE.md references to\nmatch the on-chain symbol name.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
158e9398df4b0768bb1a6c72a0165b7e82410a5b	2026-05-15T08:15:52Z	docs: lay out security audit ladder for community discussion\n\nWhat's already done (foundry, Echidna, internal AI-agent design reviews)\n+ what's open (CawProfile Echidna, C4 contest, testnet bug bounty, firm\nengagement). Each option independent, additive. Frames the decision\npoints the community can weigh in on for crowdfunding.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
e1f0284ca0a10d7adfbb2685dd788e120d46793b	2026-05-15T08:14:35Z	test(echidna): add stateful invariant fuzzing for cawonce, archive, network mgr, distribute-shape\n\nEchidna explores call-sequence space differently from foundry's per-fn\nproperty tests — finds state-machine bugs across multi-tx sequences.\nFoundry pass came back clean; this is the additive next layer.\n\n4 harnesses, 19 invariants, all passing at testLimit=50000 / seqLen=50:\n- CawonceEchidna: bitmap mirror — set-bits never clear, no spurious sets,\n  word-packing matches isCawonceUsed's reverse computation\n- DistributeShapeEchidna: shape-gate exhaustive partition (mirror of\n  foundry DistributeShapeHarness)\n- CawActionsArchiveEchidna: stake/submit/finalize/slash state machine\n  (LZ transport stubbed since OApp constructor needs a real endpoint)\n- CawNetworkManagerEchidna: ownership, fees, fee-lock, ownership-lock,\n  gas-override, instance-registration invariants\n\nConfig in solidity/echidna.yaml: testLimit 50000, seqLen 50, via-ir +\noptimizer-runs 1 matching the production build.\n\nRun individually: `echidna solidity/echidna/<file>.sol --contract <Name>\n--config solidity/echidna.yaml`. Cawonce run takes ~11min on default; can\nshrink SENDERS/CAWONCES for CI use.\n\nKnown follow-up: CawProfileL2 / CawProfile (stake / withdraw / session\nmath) is the highest-value remaining target but needs a ~200-line OApp\nmirror — separate task.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
5e4f5e67902d9193b30ef66df4999a2451d959b0	2026-05-15T07:31:04Z	chore(validator): drop unused Wallet import\n\nLeftover from the signer-abstraction migration in 433072b0 — the\nservice no longer constructs ethers.Wallet directly, so the import\nis dead.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
7e6fb2113881821d058b713a61a78b2cbfdf6ed9	2026-05-15T07:28:02Z	fix(translate): translate poll option labels with the post body\n\nPreviously runTranslation passed only useItem.content to the translator,\nso poll option labels stayed in the source language while the post body\ntranslated. Now appends options to the body as separate newline-delimited\nlines, sends as a single translate call, and splits the response by line\ncount to recover translated labels. Display-only — vote payloads still\nuse poll.options indices, never the translated text. On line-count\nmismatch (translator collapsed/split a blank line) we fall back to\noriginals + log a warn. PollDisplay accepts an optionLabelsOverride\nprop and switches the label class from truncate to break-words so\nlonger translations wrap instead of clip.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
d2e462f2e7043f456db49d3354b146dd5d8ff4ef	2026-05-15T07:27:46Z	fix(profile-chooser): scope pending counter by tokenId so siblings don't bleed\n\nThe pending-spend counter in ProfileChooser was reading the wallet-wide\npendingSpend sum from pendingSpendStore + summing ALL balance-change\nwindows from balanceChangeStore. Two profiles on the same wallet showed\neach other's spends.\n\nPer-token isolation:\n- pendingSpendStore: new tokenIdByTxQueue map; addPendingSpend accepts an\n  optional senderId. The wallet-wide pendingSpend total is preserved (the\n  mobile counter is wallet-scoped). New getPendingSpendForToken() helper.\n- balanceChangeStore: BalanceWindow gains an optional tokenId; untagged\n  windows stay visible to wallet-wide consumers (BalanceChangeToast) and\n  invisible to per-token getters.\n- ProfileChooser: subscribes to the per-txQueue maps + filters incoming\n  windows by activeToken.tokenId. The wallet-wide sums remain available\n  for other consumers (mobile counter, BalanceChangeToast).\n\nCallers tagged with senderId / activeTokenId:\n- actions.ts addPendingSpend in the submit path\n- FeedItem cancel-like 409 restore + cancel-recaw 409 restore\n- useFollowButton cancel\n- useIncomingBalanceWatcher (no-op tag — wallet-wide signal stays\n  wallet-wide, ProfileChooser only filters out cross-token leakage)\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
14768005748c0057f186e825d4bae9a500c5a375	2026-05-15T06:59:52Z	fix(og): +5% char-budget for Latin-only cards (keep base for CJK)\n\nPosts without any CJK glyph get a 1.05× multiplier on both narrow and\nwide char budgets, so wrapped lines run closer to the column's actual\nedge. Mixed-case Latin text averages ~0.47em/char in Inter while\nCHAR_W_RATIO conservatively budgets 0.50; the bump recovers most of\nthat headroom without bringing back the caps-overflow problem\n5af6e1b9 was patching.\n\nCJK posts stay at the base budget — their 2-weighted glyphs already\nconsume the full column width and over-budgeting would push lines\nback under the corner image, which c89a0ea6 just fixed.\n\nDetection via the existing glyphWeight() === 2 sweep over cleanText\nin planCawCard; one-time pass, no perf concern. Cache key v36→v37.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
22c81a9f006d2f0db423c45460b4a683b03c9d33	2026-05-15T06:46:17Z	fix(og): tighten CARD_W 970→870, closing remaining text↔image gap\n\nCaw 66 still showed a ~140-200px visible gap between wrapped narrow\ntext and the corner image even after CARD_W dropped to 970. Cause:\nCHAR_W_RATIO=0.58 (set conservatively in 5af6e1b9 to handle all-caps\noverflow) is significantly too wide for typical mixed-case content,\nso wrap budgets size for ~17px/char while real glyphs average ~14px.\nNarrow text wraps well short of the declared budget, leaving empty\nspace the image then hugs the far right edge of.\n\nDrop CARD_W to 870. CHAR_W_RATIO stays at 0.58 (still need it for\ncaps-heavy posts). The image right-anchor moves left with CARD_W, so\nthe visible text→image gap shrinks proportionally. Cache key v34→v35.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
a0cbd88840fdc8c1cccaf16b46423530d6f06b01	2026-05-15T06:35:12Z	docs: align with post-rename + post-LZ-replication contract state\n\nThe Client → Network rename (a72ca70a) also removed the dead LZ-batch\nreplication code (clientReplications mapping, addReplication setters,\nsyncReplication, ClientChainsSet, CawActionsReplicator.sol). Several\ndocs still described those paths as live or as pending cleanup.\n\nUpdates:\n  - README.md, docs/README.md, solidity/README.md, docs/ARCHITECTURE.md:\n    file/contract refs renamed; replication section rewritten around\n    optimistic-archive + CawChallengeRelay.\n  - docs/MULTI_CHAIN_STORAGE.md: footnote refresh; closed the "remove\n    clientReplications" backlog reference.\n  - PROJECT_BACKLOG.md: removed the entire "Remove clientReplications\n    from CawClientManager" cleanup section (work is done); renamed five\n    other CawClientManager → CawNetworkManager refs to match source.\n  - solidity/docs/CLIENT_REPLICATION_GUIDE.md: prepended an obsolete\n    banner pointing readers at the current optimistic-archive flow.\n\ndocs/AUDIT_2026_05_08.md intentionally left alone — historical record.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
f78bec039cb7899afa3a37b8a7b93b95b80cc469	2026-05-15T06:27:26Z	fix(og): tighten card width (CARD_W=970), center inside canvas wrap\n\nCaw cards rendered at the full 1200px OG canvas left a lot of empty\nspace between the wrapped text and the right-anchored corner image +\nstat row. Visible as ~230px of unused right-margin on /api/og/image/caw/66\nand similar.\n\nIntroduce CARD_W = 970 as the inner-card width, separate from W = 1200\n(the canvas-wrap outer dimensions Twitter/FB/Messenger expect). All\nthe geometry constants (CARD_STRIP_W, CARD_MARGIN, CARD_IMG_SZ,\nCARD_NARROW_W, CARD_WIDE_W, IMG_MAX_BY_WIDTH, narrowRenderW,\ncomputeNarrowChars, and planImageOnlyCard's cardW) now use CARD_W,\nso the image + stats sit ~25-30px from the right edge instead of\nhanging off in empty space.\n\nrenderToPng learns a width parameter (defaults to CARD_W). The caw\nimage route picks:\n  - canvas UAs (Twitter / FB / Messenger): render at W=1200 with the\n    CARD_W card centered inside; wrapTreeInOgCanvas now positions\n    the inner card with left = (W - CARD_W) / 2 instead of left = 0,\n    and gains shadows on all four sides (was top/bottom only since\n    the inner used to fill the full width).\n  - non-canvas UAs (TG / Discord / iMessage / browsers): render at\n    CARD_W directly — no neutral bands, edge-to-edge content.\n\nCache key bumped caw-v33 → caw-v34. Verified:\n  Mozilla/5.0   → 1940 × <varies> (CARD_W × 2 at 2× DPR)\n  facebookexternalhit → 2400 × 1260 (W × H × 2)\nvisible inspection: image+stats no longer floating in empty space,\ncanvas-wrap card properly centered with all-around shadow.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
c89a0ea68bb8ce582194a5601fec0be67d671afa	2026-05-15T06:15:55Z	fix(og): CJK-aware wrap so Japanese/Chinese/Korean cards don't overflow\n\nThe wrap budget (narrowChars / wideChars) was pure character count.\nCJK glyphs in Inter + Noto JP are ~2× the width of Latin glyphs, so a\n60-char narrow budget that comfortably holds Latin text overflowed\nthe column with 60 CJK chars — the line ran under the corner image,\nvisibly cut off (reported on /users/zinsanjp/caw/112154...).\n\nTwo changes in og.ts:\n\n1. weightedLength() / glyphWeight(): each codepoint contributes 1 for\n   Latin / halfwidth katakana / accented Latin, 2 for the CJK +\n   fullwidth + hangul blocks (hiragana, katakana, kanji, han, hangul,\n   fullwidth ASCII, supplementary CJK planes B-F). Used everywhere\n   the wrapper previously used .length.\n\n2. wrapCawContent: tokenize() splits Latin runs on whitespace\n   (unchanged) and CJK runs per-glyph (new — CJK has no spaces, so\n   the wrapper has to be free to break between any two characters).\n   When stitching a CJK token into the current line, no space is\n   inserted; Latin tokens keep their space separator. Hard-break of\n   over-budget tokens walks codepoints and shrinks until the\n   accumulated weight fits.\n\nEllipsis-overflow trim also uses weighted length so a Japanese line\nthat filled the budget doesn't get a spurious "…" appended.\n\nCache key bumped caw-v32 → caw-v33 so existing JP renders refresh.\nVerified on caw 112154 (zinsanjp): JP text wraps cleanly in the\nnarrow column, switches to wide width past the image.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
00f33f2fc9c8596932669995012ec6021736af34	2026-05-15T06:10:24Z	fix(share-modal): portal to body so it escapes feeditem stacking\n\nShareModal was rendered as a child of FeedItem; sibling FeedItems\nlower in the DOM rendered after the modal's parent and stacked\non top of the fixed-position modal, even at z-[80]. position: fixed\ndoesn't escape ancestor stacking contexts. createPortal to\ndocument.body fixes all ShareModal callsites in one shot\n(FeedItem + ShareProfileCardModal).\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
f3340965a6ed14d0eaf64b10237781d73b0cab40	2026-05-15T05:52:05Z	fix(prerender): keep og:image* group contiguous (FB OG parser strict)\n\nFB debugger reported "Inferred Property" + showed parsed properties\nprefixed bizarrely (og:temporal:twitter:card, og:temporal:twitter:image)\ninstead of the expected og:image / og:image:width / og:image:height.\nCause: the hreflang block of 24 <link rel="alternate">  lines was\nemitted BETWEEN og:image and its og:image:width / og:image:height /\nog:image:type / og:image:alt siblings. FB's OG parser treats any\nnon-og:* tag as the end of the current og group; the later\nog:image:* tags then get reassigned to whatever the parser's current\nproperty was, surfacing as garbled og:temporal:twitter:* entries and\nproducing the "og:image properties not yet available" warning despite\nthe tags being present.\n\nReorder buildMetaTags so the og:image* family (image, width, height,\ntype, alt) is fully contiguous, with the hreflang <link> block emitted\nAFTER it. og:site_name and twitter:* tags continue after the hreflang\nblock — they're standalone, no grouping concern.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
e4db0a64b0a5ad4185c273e08ef92ee6d02b63e3	2026-05-15T05:41:04Z	fix(prerender): probe image dims with the requesting crawler's UA\n\nThe og/image/:id route serves different shapes depending on UA: FB /\nTwitter / Messenger get the 1200×630 canvas wrap (2400×1260 at 2× DPR);\nTG / Discord / browsers get the natural variable-height card. The\nprerender probes the image with a HEAD to read x-image-width /\nx-image-height so og:image:width / og:image:height match — but the\nprobe was running with bare `node` UA (no headers passed through),\nlanding in the non-canvas branch.\n\nResult: FB scraped the HTML, saw declared og:image:width=2400\nog:image:height=608, then fetched the image (with its own UA) and got\n2400×1260. Mismatch → FB's "Inferred Property: 'og:image' properties\nare not yet available" warning + empty preview, despite a perfectly\nvalid PNG end-to-end.\n\nForward req.headers['user-agent'] into probeImageDims's fetch headers\nso the probe lands in the same branch the crawler will. Verified\ndeclared dims now match served dims across Twitter / FB / Meta-\nExternalAgent (all canvas, 2400×1260), TG / Discord (natural).\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
7dcae925c64b379b8e814c9948ac4d1b4ba2481a	2026-05-15T05:34:22Z	feat(og): canvas wrap for Facebook + Messenger too (TG/Discord stay natural)\n\nFB and Messenger enforce ≈1.91:1 OG aspect; off-aspect images get\nletterboxed or rejected outright on Messenger. Extend the\nisTwitterUA gate to also match facebookexternalhit /\nMeta-ExternalAgent so they get the fixed 1200×630 (2× DPR: 2400×1260)\ncanvas wrap that Twitter has had since 4a355ada.\n\nAlso tighten the regex: TelegramBot advertises as "TelegramBot\n(like TwitterBot)", and the old /Twitterbot/i pattern was sweeping\nTG into the canvas group too. Anchor on a leading boundary so the\nUA must START with the platform name, not just contain it as a\nmarketing nod. Verified by UA matrix:\n\n  Twitterbot/1.0                  → 2400×1260 (canvas)\n  facebookexternalhit/1.1         → 2400×1260 (canvas)\n  Meta-ExternalAgent/1.0          → 2400×1260 (canvas)\n  TelegramBot (like TwitterBot)   → 2400×608  (natural)\n  Discordbot/2.0                  → 2400×608  (natural)\n\nCache key v31→v32 with variant 'tw'→'canvas' to reflect the wider\nscope.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
05afe54146dd80e27e8b33e8ca6146896938f3bf	2026-05-15T05:24:33Z	fix(prerender): always emit og:image:width/height (FB Inferred Property)\n\nFB's debugger surfaced "Inferred Property — provided 'og:image'\nproperties are not yet available because new images are processed\nasynchronously. To ensure shares of new URLs include an image, specify\nthe dimensions using 'og:image:width' and 'og:image:height' tags."\n\nCause: probeImageDims has a 1500ms HEAD-probe timeout. First-share-of-\na-URL hits a cold satori-render cache; the probe times out; meta-build\nomits the size tags. FB's scraper then gives up on the image fetch and\n"Messenger share has no preview" is the visible symptom.\n\nAlways emit width/height. Real probed dims when available, fall back\nto the OG-spec default (1200×630) on probe miss — FB only needs\nplausible dims to accept the preview, and the real fetch supplies the\nactual size. Previously the comment claimed "never lie — a mismatched\nvalue suppresses the preview"; in practice every major scraper treats\ndeclared dims as a hint, not a strict assertion, and FB's explicit\nwarning here is asking for the hint.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
8b2bfb922ccf87e0965c340bff68c4c3ba9939e6	2026-05-15T05:14:04Z	fix(og): restore Twitter-only canvas wrap (revert overreach in 7c33c501)\n\n7c33c501 unified everyone on the fixed 1200×630 canvas after\nmisdiagnosing TG's loading-forever as an aspect-ratio issue — the real\ncause was canonical→404 (fixed in 78bceb38). Canvas-for-all left\nnon-Twitter cards with a visible ~155px padding band above and below\nthe content (e.g. a 500px-tall card rendered into 630px → 65px top,\n65px bottom). Restore the Twitter-only gate from 4a355ada so TG / FB\n/ Discord / iMessage / browsers see the natural variable-height card\nwith no padding, while X keeps the canvas wrap that defeats its\nsummary_large_image central crop.\n\nBump cache key to v31 with UA variant back in the key. Verified\ncontent-only height: caw 42527 now serves 2400×608 (was 2400×630\nwith band).\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
78bceb38677739bc3e58d5d8bb6bd88cc1b0771f	2026-05-15T05:03:58Z	fix(prerender): self-canonical until caw.social itself serves content\n\nbee1cf47 pinned rel=canonical + hreflang alternates at https://caw.social\nto collapse all CAW mirrors onto a single search-indexed entry. The\ntrouble: that origin currently 404s the actual content paths (caw.social\nDNS points at the same VPS but only matches the s.caw.social server\nblock, no /users/... handler). Telegram follows rel=canonical to\nre-fetch metadata; the 404 there makes TG bail on the preview entirely\n(infinite-loading symptom). Confirmed in nginx error log: 149.154.161.x\n(Telegram) hitting host=caw.social → 404 from /usr/share/nginx/html/.\n\nWalk it back to self-canonical (publicUrl()) for now. The\ncross-mirror-collapse intent still has merit — restore the\n'https://caw.social' pin once caw.social is a real serving host (or\nredirects to whichever mirror is canonical for that path).\n\nog:url change from 5a016e9c already pointed at the serving mirror;\nthis brings rel=canonical and hreflang in line.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
7c33c5010b70f575170130cc3d2512001a2bd349	2026-05-15T04:55:20Z	fix(og): always wrap caw card in fixed 1200×630 canvas (not just Twitter)\n\n4a355ada gated the canvas wrap on isTwitterUA on the theory that other\nplatforms read PNG natural dimensions and would render variable-height\ncards fine. Turns out Telegram in particular bails on the preview\nentirely when the image is meaningfully off-aspect from ~1.91:1 — a\n2400×984 (2.44:1) render for caw 101428 reproduced as\ninfinite-loading TG previews despite a perfect 200 response with\nvalid og:* meta.\n\nDrop the UA gate. Every preview fetcher (TG, FB, X, Discord, LinkedIn,\niMessage) targets ~1200×630, so we now serve everyone the wrapped\n2400×1260 (2× DPR) canvas. Direct-browser raw-URL viewers get a\nslightly more padded card than before — acceptable.\n\nBump cache key to v30 and drop the now-redundant UA variant in the\nkey. Also clean up the unused isTwitterUA / renderHeight branches.\n\nVerified: image actually serves 2400×1260 PNG; og:image:width /\nog:image:height meta match.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
5a016e9cc9e6471bf12eab012ba15078f812bbce	2026-05-15T04:43:31Z	fix(prerender): og:url points at serving mirror, not the canonical\n\nbee1cf47 pinned both rel=canonical AND og:url to https://caw.social\n(the cross-mirror SEO-collapse origin). That's correct for canonical\n— search engines treat the variants as one page — but wrong for\nog:url. Telegram (and other preview fetchers) actually follow og:url\nto re-fetch metadata; when caw.social isn't serving content yet, that\nfetch 404s and the preview hangs on infinite-loading despite the\nserving mirror returning a perfect prerendered page.\n\nSplit the two:\n  rel=canonical → canonicalOrigin()       (search indexing collapse)\n  og:url        → publicUrl() + path      (this serving mirror)\n\nSame fix the bee1cf47 commit message claimed it was doing\n("og:url / og:image still point at the serving mirror so social\npreviews reflect where the link came from") — the code actually\ndidn't.\n\nVerified end-to-end on test.caw.social: with og:url pointing back at\nthe same host, TG's preview fetcher gets a 200 instead of a 404.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
